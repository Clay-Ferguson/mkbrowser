# About 'pdf-export' Folder

When this application exports to a PDF format it relies on running the `generate-pdf` shell script in this folder, and so the 'lua' and 'yaml' files can be edited, manually in this folder but are not yet configurable by the user. These files get copied into the distribution as part of the install and build process  